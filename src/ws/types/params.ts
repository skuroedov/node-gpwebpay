export enum PaymentStatusState {
    REQUESTED = 1,
    PENDING = 2,
    CREATED = 3,
    APPROVED = 4,
    APPROVE_REVERSED = 5,
    UNAPPROVED = 6,
    DEPOSITED_BATCH_OPENED = 7,
    DEPOSITED_BATCH_CLOSED = 8,
    ORDER_CLOSED = 9,
    DELETED = 10,
    CREDITED_BATCH_OPENED = 11,
    CREDITED_BATCH_CLOSED = 12,
    DECLINED = 13,
    DECLINED_IN_FDS = 14,
    CANCELED = 20,
    AUTO_CANCELED = 21,
    PUSH_CREATED = 100,
    PUSH_EXPIRED = 101,
    PUSH_CANCELED = 102,
    PUSH_BLOCKED = 103,
    PUSH_PROCESSED = 110,
    WAIT_FOR_FINALIZE = 200,
    ABANDONED = 201,
    AUTO_CANCELED_210 = 210,
    AUTO_CLOSED = 211,
    REC_CREATED = 220,
    REC_VALID = 221,
    REC_CANCEL_MERCHANT = 222,
    REC_CANCEL_ISSUER = 223,
    REC_EXPIRED = 224,
    TECHNICAL_PROBLEM = 1000,
}

export enum PaymentStatus {
    PENDING_AUTHORIZATION = 'PENDING_AUTHORIZATION',
    UNPAID = 'UNPAID',
    PENDING_CAPTURE = 'PENDING_CAPTURE',
    REVERSED = 'REVERSED',
    CAPTURED = 'CAPTURED',
    PENDING_ADJUSTMENT = 'PENDING_ADJUSTMENT',
    PARTIAL_PAYMENT = 'PARTIAL_PAYMENT',
    REFUNDED = 'REFUNDED',
}

export enum PaymentStatusSubstatus {
    INITIATED = 'INITIATED',
    PGW_PAGE = 'PGW_PAGE',
    ThreeDS_REDIRECT = '3DS_REDIRECT',
    ThreeDS_SUBMIT = '3DS_SUBMIT',
    PAYMENT_REDIRECT = 'PAYMENT_REDIRECT',
    MPS_SCH_REDIRECT = 'MPS_SCH_REDIRECT',
    MPS_SCH_SUBMIT = 'MPS_SCH_SUBMIT',
    MPS_SCH_CANCEL = 'MPS_SCH_CANCEL',
    DEFERRED_SUBMIT = 'DEFERRED_SUBMIT',
    CANCELED = 'CANCELED',
    TECHNICAL_PROBLEM = 'TECHNICAL_PROBLEM',
    FRAUD = 'FRAUD',
    DECLINED = 'DECLINED',
    REVERSED_BY_MERCHANT = 'REVERSED_BY_MERCHANT',
    REVERSED_BY_SYSTEM = 'REVERSED_BY_SYSTEM',
    DEFERRED_REDIRECT = 'DEFERRED_REDIRECT',
    PENDING_CAPTURE_SETTLEMENT = 'PENDING_CAPTURE_SETTLEMENT',
    PENDING_REFUND_SETTLEMENT = 'PENDING_REFUND_SETTLEMENT',
    SENT_TO_SETTLEMENT = 'SENT_TO_SETTLEMENT'
}
